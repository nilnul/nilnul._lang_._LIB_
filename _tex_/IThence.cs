namespace nilnul.lang._tex_
{
	/// <summary>
	/// without the current token, parse forward till it's not possible (the parsing can stop any where, even advance 0 step, leaving the cursor still at its original position), for a symbol.
	/// the history of past symbols can be stored as a environment state, or as the input, which can be consumed by the compilation.
	/// </summary>
	/// alias:
	public interface IThence
    {
    }

}
